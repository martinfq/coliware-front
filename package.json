{
  "name": "money-landing",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler pages deploy dist --project-name=money-landing"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "astro": "^6.3.7",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "wrangler": "^4.94.0"
  }
}
